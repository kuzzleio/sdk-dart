name: collection#updateMapping
description: Update the collection mapping
hooks:
  before: curl -X POST kuzzle:7512/nyc-open-data/_create && curl -X PUT kuzzle:7512/nyc-open-data/yellow-taxi
  after:
template: print-deprecations
expected: [
  {
      "message": "This action has been deprecated since Kuzzle version 2.1.0. This feature might be removed in a future major version. To update a collection, use this API action instead: \"collection:update\".",
      "version": "2.1.0"
  }
]
