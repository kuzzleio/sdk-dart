name: kuzzle
description: A library to interact with kuzzle API. A backend software, self-hostable and ready to use to power modern cross-platform apps.
homepage: https://github.com/kuzzleio/sdk-dart
version: 2.3.0-nullsafety.0

environment:
  sdk: ">=2.12.0-0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Sound null safety ready
  http: ^0.13.0
  meta: ^1.3.0
  pedantic: ^1.10.0
  uuid: ^3.0.0

dev_dependencies:
  coverage: ^0.15.2
  test: ^1.16.4
